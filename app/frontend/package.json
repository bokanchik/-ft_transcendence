{
  "name": "frontend",
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p dist && postcss src/style.css -o dist/style.css && cp index.html dist/index.html && cp src/main.js dist/main.js"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.0",
    "postcss": "^8.4.0",
    "postcss-cli": "^9.0.0",
    "tailwindcss": "^3.0.0"
  }
}
