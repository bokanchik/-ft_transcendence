{
  "name": "frontend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:clean": "rm -rf ./dist",
    "build:html": "mkdir -p ./dist && cp ./src/index.html ./dist/index.html && cp ./src/users.html ./dist/users.html",
    "build:ts": "tsc",
    "build:css": "npx tailwindcss -i ./src/input.css -o ./dist/css/style.css --minify",
    "build": "npm run build:clean && mkdir -p ./dist/css ./dist/js && npm run build:html && npm run build:ts && npm run build:css",
    "watch:ts": "tsc --watch",
    "watch:css": "npx tailwindcss -i ./src/input.css -o ./dist/css/style.css --watch",
    "dev": "npm run build:clean && mkdir -p ./dist/css ./dist/js && npm run build:html && concurrently \"npm:watch:ts\" \"npm:watch:css\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
  }
}
